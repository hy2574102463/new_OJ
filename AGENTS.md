# OJ 项目开发协作规范

本文件是实现本仓库时的执行清单。需求依据见 [`oj/`](oj/) 文档，尤其是 `api.md`；若代码与指南冲突，应优先修正代码或记录明确的兼容决策。

## 指南细节与实现约束

- 推荐 Python 3.10、GCC 9+、C++14，并按 Linux 风格命令验证；Windows 开发优先使用 WSL。Step 2 只要求单用户提交任务正确，但评测仍须异步且不能阻塞事件循环。
- 输出比较必须拒绝额外提示语，仅忽略行末空格和最终多余换行；测试点结果限定为 `AC/WA/TLE/MLE/RE/CE/UNK`，未知异常归为 `UNK`；每个测试点 10 分。题目未提供时间/内存限制时继承语言默认值；C++ 命令先编译，`{src}`/`{exe}` 必须是有效路径。
- 题目详情缺省字段返回稳定类型默认值（字符串 `""`、列表 `[]`）；评测详情将编译信息、运行阶段信息、任务级错误和测试点日志分开返回。
- 提交限流为 1 分钟最多 3 次。列表筛选中 `user_id`、`problem_id` 为一级条件，不能同时缺省；`page`/`page_size` 同时缺省查全部，仅 `page_size` 查第一页，仅 `page` 返回 400。普通用户只能查看自己的提交，管理员可按题目查看所有用户提交；`pending`/`error` 摘要只需包含 ID 和状态。
- 日志未公开时仅本人或管理员可见；`public_cases=True` 后所有已登录用户可见测试点明细，但仍不能越权读取 Step 2/3 简单结果。审计记录用户、题目、操作、时间、状态；未登录、资源不存在、参数错误请求可不记录。
- Step 6 必须使用 Python Streamlit，页面不能直接读写后端；AI 的工具调用/Agent Loop 不是硬性要求。AI 任务必须有等待、执行、完成、中断、失败状态、持续进度和真实取消，结果要能回填题目新增/编辑。

### 指南歧义的统一约定

- 审计接口文字使用 `view_logs`，示例出现 `view_log`；实现统一采用 `view_logs`，如需兼容旧示例须在适配层处理。
- 语言注册在 API 中标注“所有已登录用户”，同时列出 403 异常；按 Step 4 明确规则允许所有已登录用户注册，统一鉴权仍保留 403 响应能力。
- AI 接口路径均为“建议路径”，不是基础模块严格契约；改用其他等价路径时，必须在 README 或 API 文档说明请求、状态和响应映射。
- `AC ~ CE` 不是严谨的范围表达；按完整状态枚举实现，其他运行/系统异常归为 `UNK`。

## 教学优先：学习而非代写

本项目的首要目标是让学生理解 Web 服务、异步评测、权限控制和前后端协作的原理。允许使用 AI 辅助，但不得把 AI 当作不可解释的代码生成器。任何代码修改或命令操作都必须遵循以下说明义务：

1. **先说明目标**：操作前用简短文字说明要解决的问题、涉及的模块，以及它与哪一条实验要求或 API 契约对应。
2. **解释关键原理**：写代码时说明数据如何流动、为什么选择该数据结构/框架机制、异步边界在哪里、错误和权限如何传播。涉及新概念（如 Session、`asyncio.create_task`、子进程资源限制、SSE）时，先给出适合初学者的解释和最小示例。
3. **逐项解释改动**：完成后按文件和关键函数说明“改了什么、为什么这样改、有哪些替代方案及取舍”。不能只给出 diff、代码片段或“已完成”的结论。
4. **解释每条命令**：执行安装、启动、测试、Git 等命令时说明命令各部分作用、预期输出，以及该操作是否会修改数据或外部状态。避免使用学生尚未见过的长命令；必要时拆成多个可观察步骤。
5. **验证要可复现**：每项实现都要给出学生可以亲自运行的验证步骤（请求示例、测试命令或手动页面流程），说明应观察什么结果，以及失败时如何定位。测试通过不能替代理解测试为何能证明行为正确。
6. **保留思考环节**：对于架构选择、并发模型、权限策略和安全边界，先列出问题和判断依据；鼓励学生先预测结果或尝试实现，再提供提示和参考实现。除非学生明确要求完整实现，不要一次性隐藏所有推理过程或跳过基础知识。
7. **诚实标注不确定性**：如果指南存在歧义、实现只是教学简化或尚未验证，要明确标注假设、限制和潜在风险，不得把推测描述成事实。
8. **控制改动范围**：优先小步、可回滚的提交；一次修改聚焦一个概念。不得为了让测试通过而绕过鉴权、删除测试或掩盖异常。

### 代码注释与可读性要求

- 后续新增或修改的源码都必须包含与学生当前知识水平匹配的注释，使读者能理解该文件的职责以及代码在请求链路中的位置；该要求同样适用于测试中的非显然准备过程和复杂断言。
- 每个源码模块使用模块文档字符串说明“这个文件负责什么、不负责什么”；公共类、函数、异步路由和后台任务使用文档字符串说明输入、核心处理、输出及可能的异常传播。
- 在异步边界、事务提交/回滚、权限判断、状态转换、资源限制、进程清理和敏感信息处理等关键位置添加行前注释，解释为什么采用该机制以及失败时会发生什么。
- 注释应解释意图、约束和取舍，不得逐行翻译语法或添加“给变量赋值”一类无信息注释。代码行为变化时必须同步更新相关注释，错误注释视为缺陷。
- API、数据模型或跨层调用发生变化时，除代码注释外还要更新 README 或 API 文档；注释不能代替可复现测试。

### 每次开发回复的最低结构

在进行实际修改时，回复至少包含：

- **本次目标与前置知识**；
- **计划修改的文件/操作及其原理**；
- **实现后的关键代码讲解**（包括输入、处理、输出和异常路径）；
- **验证步骤与预期结果**；
- **遗留问题、替代方案或下一步练习**。

如果只是读取文件、查看状态等只读操作，也要说明它用于获取什么证据。若命令失败，应解释错误原因、尝试过的排查路径和下一步，而不是静默重试。

### 学生实践要求

- 学生应能独立回答：一个请求从路由到存储再返回响应的路径、`pending/success/error` 与 `AC/WA/...` 的区别、401 与 403 的区别、超时/超内存如何终止进程。
- 新增模块后，至少安排一个“先不看答案”的小练习（例如手写一个权限依赖、输出比较函数或分页函数），再对照实现。
- 报告中记录 AI 提示、采纳/修改的代码比例、自己完成的测试和遇到的错误；所有生成代码都必须经过人工阅读、运行和解释。

## 开发原则

- 后端路由必须使用 `async def`；耗时评测和 AI 调用放入后台任务，不能阻塞事件循环。
- 路由只做鉴权、参数解析和响应编排；业务逻辑放 `services/`，持久化放 `repositories/`，评测执行放 `judge/`。
- 所有响应包含 `code/msg/data`，状态码真实对应结果；将 FastAPI 校验错误统一映射为 400。
- 认证与授权在后端完成。角色至少为 `user/admin/banned`；禁止仅靠前端隐藏按钮实现权限。
- 密码使用 bcrypt 哈希。模型密钥、用户代码中的敏感信息、绝对路径和内部堆栈不得写入日志或普通响应。
- 用户代码运行必须隔离工作目录，限制 CPU/墙钟时间和内存，可靠清理子进程与临时文件；命令模板禁止未经校验的 shell 拼接。
- 提交状态与测试点结果分离；输出比较忽略行末空格和最终换行，保留可审计的编译/运行信息。

## 推荐工作流

1. 开始任务前阅读相关 Step 和 `oj/api.md`，确认路径、字段、权限、异常码。
2. 先补测试或验收用例，再实现最小改动；跨模块变更同步更新 README/API 注释。
3. 本地运行格式化、静态检查和测试；至少覆盖正常、参数错误、未登录、越权和资源超限场景。
4. 检查 `git diff`，确认没有密钥、数据库、编译产物或大文件；使用 Conventional Commits（`feat`、`fix`、`test`、`docs`、`refactor`）。

## Git 提交信息规范（Conventional Commits）

所有 Git 提交必须遵循 [Conventional Commits 1.0.0](https://www.conventionalcommits.org/zh-hans/v1.0.0/)：

```text
<type>[optional scope]: <description>

[optional body]

[optional footer(s)]
```

- `type` 使用小写且必填。常用类型：`feat`（新增功能）、`fix`（修复问题）、`docs`（文档）、`test`（测试）、`refactor`（不改变行为的重构）、`perf`（性能）、`build`（构建/依赖）、`ci`（持续集成）、`chore`（维护）。
- `scope` 可选，用模块名标明影响范围，例如 `api`、`judge`、`auth`、`frontend`、`ai`、`docs`。
- `description` 必填，使用祈使式、简洁描述实际变更；首字母不必大写，末尾不加句号。每次提交聚焦一个逻辑变更，不要把无关格式化混入其中。
- 需要补充背景、实现思路或迁移提示时，在正文说明“为什么改、如何验证”；正文与标题之间空一行。
- 不兼容 API 或数据格式变更必须在类型后加 `!`（如 `feat(api)!:`），并在 footer 写明 `BREAKING CHANGE: ...` 及迁移方式。
- 不要使用含糊标题（如 `update`、`修改代码`、`一些修复`），也不要在提交信息中写入密码、API 密钥或其他敏感数据。

示例：

```text
feat(judge): add subprocess timeout handling
fix(auth): reject banned users during login
test(api): cover submission permission matrix
docs: explain async evaluation workflow
refactor(storage): isolate problem repository
feat(api)!: change submission list pagination fields

BREAKING CHANGE: replace `counts` with `total_cases`; update frontend clients.
```

提交前检查：确认 `git diff` 与暂存区内容只包含本次逻辑；运行相关测试；用 `git log -1 --oneline` 检查标题格式；确认没有密钥、数据库、编译产物或大文件。提交标题建议不超过 72 个字符。若一次工作包含多个独立概念，应拆成多个按依赖顺序排列的提交，而不是用一个笼统提交掩盖变化。

## 分阶段完成定义

- Step 1：题目 CRUD、默认字段、重复 ID 和不存在资源分别返回约定结果。
- Step 2：Python 必须可评测，C++ 可编译运行；能判定 AC/WA/RE/CE/TLE/MLE；支持语言注册/查询。
- Step 3：提交异步状态可轮询；列表筛选分页；详情仅本人/管理员；管理员 rejudge 后回到 pending。
- Step 4：启动自动创建 `admin/admintestpassword`；注册校验用户名 3–40、密码至少 6 位；禁用用户不能登录。
- Step 5：每个测试点有结果、时间、内存；日志公开策略和访问审计符合权限矩阵。
- Step 6：Streamlit 用户、题目、提交三组页面均通过 API，正确保存会话并展示异常。
- Advance：模型配置实际生效；任务有 running/completed/cancelled/failed 等状态、实时进度、有效取消、Token 与费用统计。

## 协作与安全禁区

- 不要绕过 API 直接从 Streamlit 读写后端数据。
- 不要硬编码模型提供商、模型名或 API 密钥；不要在错误信息中返回服务器敏感路径。
- 不要执行未经限制的用户命令，不要把用户输入直接作为 shell 脚本解释。
- 不要修改或删除无关文件；避免破坏已有用户改动。涉及数据重置时仅使用测试专用 `POST /api/reset/`。
- 对接口契约的任何变更必须同时更新测试和文档，并说明兼容影响。

## 验收前检查

- `pytest`（或项目指定测试命令）通过，FastAPI 与 Streamlit 均可启动。
- 用 curl/Postman 验证 401/403/400/404/409/429/500 及 `{code,msg,data}`。
- 演示完整闭环，并准备 AC、WA、RE、CE、TLE、MLE 和日志可见性证据。
- 确认最后一次提交在 9 月 10 日课前完成，报告包含架构、难点、测试、成果和 AI 使用说明。
